import  React from 'react'

export default class Authenti extends React.Component{
    state={
    }

    onChange = (e)=>{
    }
    render(){
        return (
        <div>
            </div>
        );
    }
}